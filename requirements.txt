# Minimal dependencies required to run app.py / run_pipeline.py
# (inference only — training/notebook dependencies are unaffected)

pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
lightgbm>=4.0
joblib>=1.3
